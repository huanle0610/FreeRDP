services:
  freerdp-mingw:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/workspace
    working_dir: /workspace
    tty: true
    stdin_open: true
    command: >
      bash -c "./scripts/mingw.sh && ./scripts/mingw.sh -c -s --clean-first"